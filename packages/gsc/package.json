{
  "name": "opencli-plugin-gsc",
  "version": "0.1.0",
  "type": "module",
  "description": "opencli plugin for Google Search Console request indexing",
  "license": "MIT",
  "peerDependencies": {
    "@jackwener/opencli": ">=1.8.6"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  },
  "scripts": {
    "build": "npx esbuild src/lib.ts --outfile=lib.js --format=esm --platform=node && npx esbuild src/request-indexing.ts --bundle --outfile=request-indexing.js --format=esm --platform=node --packages=external"
  }
}
